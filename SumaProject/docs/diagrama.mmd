classDiagram
    class SumaApplication {
        +SpringBootApplication
        *main(args)
        <<include>> SpringApplication app
        -run(app, args)
    }

    class SumaController {
        +RestController
        -suma(num1, num2) : int
    }

    class SpringApplication {
        -run(app, args)
    }