```markdown
El propósito del código COBOL es realizar una suma simple de dos números y mostrar el resultado. El programa consiste en una sección de identificación, una sección de datos y una sección de procedimiento.

La sección de datos define tres variables: NUM1, NUM2 y RESULTADO, que representan los dos números a sumar y su resultado, respectivamente.

La sección de procedimiento contiene una única rutina llamada MAIN-PROCEDURE, que realiza los siguientes pasos:

- Pide al usuario que introduzca el primer número
- Almacena el valor introducido en NUM1
- Pide al usuario que introduzca el segundo número
- Almacena el valor introducido en NUM2
- Realiza la operación de suma y guarda el resultado en RESULTADO
- Muestra el resultado final por pantalla

Las reglas de negocio asociadas a este código son sencillas: se ingresan dos números y se obtiene su suma.
```